p=float(input("enter the principal amount:"))
r=float(input("enter therate of interest:"))
t=int(input("enter the time period:"))
si=(p*r*t)/100
if p>=1 and p<=10000 and r>=1 and r<=20 and t>=1 and t <=10:
   print("the si is:",si)
else:
   print("invalid")
